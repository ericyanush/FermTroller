os: linux
dist: trusty
language: c

compiler:
  - clang

git:
  submodules: false

before_install:
  - git submodule update --init --recursive

before_script:
  - mkdir -p tests/build
  - cd tests/build

script:
  - cmake ../
  - make
  - ./fermtrollerTests